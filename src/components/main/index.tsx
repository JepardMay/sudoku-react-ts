import React from 'react';
import Logo from '../logo';

// interface Props {
//   setView: (view: boolean) => void;
// }

function Main() {
  return (
    <div className="Main">
      <Logo mod={'big'} />
    </div>
  );
}

export default Main;
